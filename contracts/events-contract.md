# Events Contract

## Publishes
- `asset.onboarded` (schema: `AssetOnboarded`)
- `asset.assigned` (schema: `AssetAssigned`)
- `asset.archived` (schema: `AssetArchived`)

## Consumes
- `hr.user.offboarded` (schema: `UserOffboarded`)
